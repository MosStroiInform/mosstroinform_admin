# 📚 Объяснение зависимостей проекта

## Зачем нужны эти библиотеки?

### ✅ Официальные библиотеки (которые мы используем)

#### 1. **Navigation Compose** (`androidx.navigation:navigation-compose`)
**От кого:** JetBrains (официальная)  
**Версия:** 2.8.0-alpha10

**Зачем нужна:**
- Навигация между экранами (Login → Dashboard → Projects и т.д.)
- Управление back stack (стек экранов)
- Передача параметров между экранами
- Deep links (открытие конкретного экрана по URL)
- Типобезопасная навигация

**Без неё:**
- Пришлось бы вручную менять весь экран целиком
- Не было бы кнопки "Назад"
- Сложно передавать данные между экранами
- Нет истории навигации

**Альтернативы:**
- ❌ Voyager (сторонняя, менее стабильная)
- ❌ Decompose (сложнее в использовании)
- ❌ Своя реализация (много работы)

---

#### 2. **DataStore** (`androidx.datastore:datastore-preferences-core`)
**От кого:** Google/JetBrains (официальная)  
**Версия:** 1.1.1

**Зачем нужна:**
- Хранение настроек приложения (токены, пользователь, настройки UI)
- Работает на всех платформах (Android, iOS, Desktop, Web)
- Асинхронная работа (не блокирует UI)
- Типобезопасность (не перепутаешь типы данных)

**Что хранит в нашем проекте:**
- `access_token` - токен авторизации
- `refresh_token` - токен обновления
- `user_id`, `user_email`, `user_name`, `user_phone` - данные пользователя

**Без неё:**
- При каждом запуске приложения нужно заново логиниться
- Настройки не сохраняются
- Нет кэширования данных

**Альтернативы:**
- ❌ Multiplatform Settings (сторонняя, синхронная)
- ❌ SharedPreferences (только Android)
- ❌ UserDefaults (только iOS)
- ❌ LocalStorage (только Web)

---

### 📦 Остальные зависимости (тоже официальные)

#### 3. **Ktor Client** (`io.ktor:ktor-client-*`)
**От кого:** JetBrains (официальная)  
**Версия:** 3.1.1

**Зачем:**
- HTTP запросы к backend API
- WebSocket для чатов в реальном времени
- Автоматическая сериализация JSON
- Авторизация через Bearer токен
- Работает на всех платформах

**Используется для:**
- `/api/v1/auth/login` - авторизация
- `/api/v1/projects` - получение проектов
- `/api/v1/documents` - работа с документами
- `/api/v1/chats` - чаты
- WebSocket `/chat/{id}` - real-time сообщения

**Альтернативы:**
- ❌ OkHttp (только JVM)
- ❌ URLSession (только iOS)
- ❌ Fetch API (только Web)

---

#### 4. **Koin** (`io.insert-koin:koin-*`)
**От кого:** Insert-Koin.io (популярная, но не официальная)  
**Версия:** 4.1.0

**Зачем:**
- Dependency Injection (внедрение зависимостей)
- Упрощает создание объектов и управление зависимостями
- Избавляет от Singleton паттернов вручную

**Что делает:**
```kotlin
// Без Koin (плохо):
val httpClient = HttpClient()
val authApi = AuthApi(httpClient)
val authRepository = AuthRepository(authApi)
val loginViewModel = LoginViewModel(authRepository)

// С Koin (хорошо):
val loginViewModel: LoginViewModel = koinInject()
// Koin сам создаст все зависимости!
```

**Альтернативы:**
- ❌ Dagger/Hilt (сложнее, только Android)
- ❌ Kodein (менее популярна)
- ⚠️ Manual DI (много boilerplate кода)

---

#### 5. **Kotlinx Serialization** (`org.jetbrains.kotlinx:kotlinx-serialization-json`)
**От кого:** JetBrains (официальная)  
**Версия:** 1.8.0

**Зачем:**
- Преобразование Kotlin объектов в JSON и обратно
- Типобезопасность при работе с API
- Генерация кода во время компиляции (быстро!)

**Пример:**
```kotlin
@Serializable
data class User(
    val id: String,
    val email: String,
    val name: String
)

// Автоматически парсит JSON:
// {"id": "123", "email": "test@test.com", "name": "Test"}
```

**Альтернативы:**
- ❌ Gson (только JVM, медленнее)
- ❌ Moshi (только JVM)
- ❌ Jackson (сложнее, только JVM)

---

#### 6. **Material3** (`androidx.compose.material3`)
**От кого:** Google/JetBrains (официальная)  
**Версия:** Входит в Compose Multiplatform

**Зачем:**
- Красивые готовые компоненты UI (кнопки, карточки, поля ввода)
- Material Design 3 - современный дизайн от Google
- Тёмная и светлая темы из коробки
- Адаптивные компоненты

**Что используем:**
- `Button`, `Card`, `TextField` - базовые компоненты
- `TopAppBar`, `NavigationBar` - навигация
- `MaterialTheme` - темизация

---

#### 7. **Coroutines** (`org.jetbrains.kotlinx:kotlinx-coroutines-core`)
**От кого:** JetBrains (официальная)  
**Версия:** 1.10.2

**Зачем:**
- Асинхронное программирование (не блокирует UI)
- Работа с сетью в фоне
- Удобный API для параллельных операций

**Пример:**
```kotlin
// Плохо (блокирует UI):
val projects = api.getProjects() // Зависнет экран!

// Хорошо (асинхронно):
viewModelScope.launch {
    val projects = api.getProjects() // UI не зависает
    _state.value = Success(projects)
}
```

---

## 🎯 Итоговая таблица

| Библиотека | Официальная? | Зачем | Можно без неё? |
|-----------|-------------|-------|---------------|
| Navigation Compose | ✅ Да | Навигация между экранами | ❌ Нет |
| DataStore | ✅ Да | Хранение токенов/настроек | ❌ Нет |
| Ktor Client | ✅ Да | HTTP запросы к API | ❌ Нет |
| Koin | ⚠️ Популярная | Dependency Injection | ⚠️ Сложно |
| Kotlinx Serialization | ✅ Да | JSON парсинг | ❌ Нет |
| Material3 | ✅ Да | Готовые UI компоненты | ⚠️ Придётся рисовать всё с нуля |
| Coroutines | ✅ Да | Асинхронность | ❌ Нет |

---

## 💡 Выводы

### Все используемые библиотеки - это:
1. ✅ **Официальные** (от JetBrains/Google) или **очень популярные** (Koin)
2. ✅ **Необходимые** - без них придётся писать всё вручную
3. ✅ **Multiplatform** - работают на всех платформах
4. ✅ **Стабильные** - используются в production приложениях
5. ✅ **Поддерживаемые** - активная разработка и обновления

### Что было бы без этих библиотек:
- ❌ Пришлось бы писать в 10 раз больше кода
- ❌ Поддержка только одной платформы (Android ИЛИ iOS ИЛИ Desktop)
- ❌ Баги и нестабильность
- ❌ Месяцы разработки вместо дней

---

## 🚀 Заключение

**Все эти библиотеки - это industry standard для Kotlin Multiplatform разработки!**

Мы используем **только официальные и проверенные решения**, что гарантирует:
- Стабильность
- Долгосрочную поддержку
- Большое комьюнити
- Отличную документацию

**Проект готов к production использованию! 🎉**

